language: python
notifications:
  irc:
    channels:
      - "irc.freenode.net#gplaycli"
      - "chat.freenode.net#gplaycli"
    on_success: always
    on_failure: always
python:
  - "2.7"
  - "pypy-5.3.1"
before_install: 
  - date
install:
  - pip install .
  - pip install -r requirements.txt
# command to run tests
script: py.test
